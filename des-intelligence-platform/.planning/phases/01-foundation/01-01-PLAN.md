---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - knowledge/.gitkeep
  - knowledge/intelligence/.gitkeep
  - knowledge/intelligence/briefs/.gitkeep
  - audit/submissions/.gitkeep
  - proposals/.gitkeep
  - content/briefs/.gitkeep
  - content/drafts/.gitkeep
  - content/published/.gitkeep
  - .claude/skills/.gitkeep
autonomous: true
requirements:
  - FOUND-01

must_haves:
  truths:
    - "All required project directories exist and are tracked by git"
    - "The .claude/skills/ directory is ready to receive project-local skill files"
    - "The knowledge/intelligence/ subtree exists for the Trends Engine (Phase 6)"
  artifacts:
    - path: "knowledge/"
      provides: "Root knowledge base directory"
    - path: "knowledge/intelligence/"
      provides: "Intelligence layer subdirectory for trends and briefs"
    - path: "knowledge/intelligence/briefs/"
      provides: "Content brief storage for /des-trends output"
    - path: "audit/submissions/"
      provides: "AuditResult output directory for /des-audit"
    - path: "proposals/"
      provides: "HTML proposal output directory for /des-proposal"
    - path: "content/briefs/"
      provides: "Content brief staging directory"
    - path: "content/drafts/"
      provides: "Blog post draft directory for /des-content"
    - path: "content/published/"
      provides: "Published content log directory"
    - path: ".claude/skills/"
      provides: "Project-local Claude Code skill directory"
  key_links:
    - from: ".claude/skills/"
      to: "Claude Code skill loader"
      via: "project-local skill resolution"
      pattern: ".claude/skills/.*SKILL\\.md"
---

<objective>
Initialize the complete DES Intelligence Platform directory structure so all subsequent phases have stable, git-tracked output destinations.

Purpose: Every skill, plan, and phase after this one writes output to a known path. This plan creates those paths. Without them, Phase 2+ tasks have nowhere to write and verification checks will fail.

Output: Nine directories created and tracked via .gitkeep placeholders. The .claude/skills/ directory is ready for Phase 2's /des-interview skill definition.
</objective>

<execution_context>
@/Users/erick/claude-examples/des-intelligence-platform/.planning/PROJECT.md
</execution_context>

<context>
@/Users/erick/claude-examples/des-intelligence-platform/.planning/ROADMAP.md
@/Users/erick/claude-examples/des-intelligence-platform/.planning/PROJECT.md

Project root: /Users/erick/claude-examples/des-intelligence-platform

The repository currently has only: .planning/, .claude/settings.local.json, CONTEXT.md, PROJECT.md, and source reference files (DentalEdgeSolutions_Proposal.html, DES Plan Basics.docx). No output directories exist yet.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all project directories with .gitkeep placeholders</name>
  <files>
    knowledge/.gitkeep
    knowledge/intelligence/.gitkeep
    knowledge/intelligence/briefs/.gitkeep
    audit/submissions/.gitkeep
    proposals/.gitkeep
    content/briefs/.gitkeep
    content/drafts/.gitkeep
    content/published/.gitkeep
    .claude/skills/.gitkeep
  </files>
  <action>
    Create the following directories under the project root /Users/erick/claude-examples/des-intelligence-platform/ by writing empty .gitkeep files at each path (git does not track empty directories):

    Directories to create:
    - knowledge/                    — root knowledge base; will hold services.md, company-profile.md, market-study.md, benchmarking.md, ghl-config.json
    - knowledge/intelligence/       — intelligence layer; will hold sources.md, trends.md
    - knowledge/intelligence/briefs/ — content briefs produced by /des-trends
    - audit/                        — practice audit system root
    - audit/submissions/            — AuditResult .md files from /des-audit
    - proposals/                    — HTML proposal output from /des-proposal
    - content/                      — content pipeline root
    - content/briefs/               — content brief staging
    - content/drafts/               — blog post drafts from /des-content
    - content/published/            — published post logs with GHL post IDs
    - .claude/skills/               — project-local Claude Code skills (all 5 DES commands)

    Write a .gitkeep file (empty, zero bytes) in each leaf directory so git tracks the path.

    Do NOT create .gitkeep in intermediate directories that already have a child with .gitkeep — git will track the parent automatically.

    Use Bash to create directories and touch .gitkeep files:
    mkdir -p knowledge/intelligence/briefs audit/submissions proposals content/briefs content/drafts content/published .claude/skills
    Then touch .gitkeep in each of: knowledge/, knowledge/intelligence/, knowledge/intelligence/briefs/, audit/submissions/, proposals/, content/briefs/, content/drafts/, content/published/, .claude/skills/

    Run all mkdir and touch commands from the project root: /Users/erick/claude-examples/des-intelligence-platform
  </action>
  <verify>
    <automated>cd /Users/erick/claude-examples/des-intelligence-platform && ls knowledge/ knowledge/intelligence/ knowledge/intelligence/briefs/ audit/submissions/ proposals/ content/briefs/ content/drafts/ content/published/ .claude/skills/</automated>
  </verify>
  <done>All nine directories exist under the project root. Each contains a .gitkeep file. git status shows the new .gitkeep files as untracked (ready to stage).</done>
</task>

<task type="auto">
  <name>Task 2: Stage and commit directory scaffold</name>
  <files>
    knowledge/.gitkeep
    knowledge/intelligence/.gitkeep
    knowledge/intelligence/briefs/.gitkeep
    audit/submissions/.gitkeep
    proposals/.gitkeep
    content/briefs/.gitkeep
    content/drafts/.gitkeep
    content/published/.gitkeep
    .claude/skills/.gitkeep
  </files>
  <action>
    From the project root /Users/erick/claude-examples/des-intelligence-platform, stage and commit all .gitkeep files:

    git add knowledge/.gitkeep knowledge/intelligence/.gitkeep knowledge/intelligence/briefs/.gitkeep audit/submissions/.gitkeep proposals/.gitkeep content/briefs/.gitkeep content/drafts/.gitkeep content/published/.gitkeep .claude/skills/.gitkeep

    Commit message: "feat(foundation): initialize project directory structure"

    Do not add the full DentalEdgeSolutions_Proposal.html or docx files if they are not already tracked — only add the .gitkeep files listed above.
  </action>
  <verify>
    <automated>cd /Users/erick/claude-examples/des-intelligence-platform && git log --oneline -1</automated>
  </verify>
  <done>git log shows the scaffold commit as the most recent commit. git status is clean (no uncommitted changes from this plan).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → git | Empty placeholder files committed; no secrets or PII at this stage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | .gitkeep files | accept | Zero-byte placeholders; no attack surface |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After the commit, confirm:
1. `ls knowledge/ knowledge/intelligence/ knowledge/intelligence/briefs/ audit/submissions/ proposals/ content/briefs/ content/drafts/ content/published/ .claude/skills/` — all directories respond without "No such file" errors
2. `git log --oneline -1` shows the scaffold commit
3. `git status` shows nothing uncommitted
</verification>

<success_criteria>
All directories from the ROADMAP Phase 1 success criterion #1 exist and are tracked by git. Subsequent plans in this phase can write files to these paths without first creating directories.
</success_criteria>

<output>
Create .planning/phases/01-foundation/01-01-SUMMARY.md when done, documenting: directories created, commit hash, any deviations from the plan.
</output>
