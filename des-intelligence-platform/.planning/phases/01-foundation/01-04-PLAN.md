---
phase: 01-foundation
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - knowledge/data4seo-test.json
autonomous: true
requirements:
  - FOUND-04

must_haves:
  truths:
    - "Data4SEO sandbox returns a valid 20000 status response for a Maps SERP query on a test domain"
    - "Data4SEO sandbox returns a valid 20000 status response for a Business Data my_business_info query"
    - "Credentials DATA4SEO_LOGIN and DATA4SEO_PASSWORD are confirmed present in environment"
    - "knowledge/data4seo-test.json records the test results for future reference"
  artifacts:
    - path: "knowledge/data4seo-test.json"
      provides: "Sandbox verification record with response samples from both tested endpoints"
      contains: "endpoint tested, status_code, sample result fields, cost, timestamp"
  key_links:
    - from: "DATA4SEO_LOGIN / DATA4SEO_PASSWORD env vars"
      to: "sandbox.dataforseo.com"
      via: "Basic auth (base64 login:password)"
      pattern: "sandbox\\.dataforseo\\.com"
---

<objective>
Verify that Data4SEO sandbox credentials work and that both endpoints required by /des-audit return valid data — Maps SERP (for local rankings and competitor discovery) and Business Data my_business_info (for GBP review count and rating).

Purpose: /des-audit depends entirely on Data4SEO for prospect enrichment. A broken credential or wrong endpoint shape discovered in Phase 4 would block the Proposal Engine. Verifying now with sandbox calls costs nothing and de-risks all subsequent phases.

Output: knowledge/data4seo-test.json confirming both endpoints return status_code 20000 from the sandbox environment.
</objective>

<execution_context>
@/Users/erick/claude-examples/des-intelligence-platform/.planning/PROJECT.md
</execution_context>

<context>
@/Users/erick/claude-examples/des-intelligence-platform/.planning/ROADMAP.md
@/Users/erick/claude-examples/des-intelligence-platform/.planning/research/DATA4SEO.md

Data4SEO authentication:
- HTTP Basic Auth: base64(DATA4SEO_LOGIN + ":" + DATA4SEO_PASSWORD)
- Sandbox base URL: https://sandbox.dataforseo.com (not api.dataforseo.com)
- Sandbox returns mock data — no credits consumed. Always use sandbox during development.
- Credentials are in environment variables: $DATA4SEO_LOGIN and $DATA4SEO_PASSWORD

Endpoint A — Maps SERP (local rankings + competitor list):
POST https://sandbox.dataforseo.com/v3/serp/google/maps/live/advanced
Body: [{"keyword": "dentist miami", "location_code": 1015116, "language_code": "en", "device": "desktop", "os": "windows"}]
Expected: tasks[0].status_code == 20000, items array with rank_absolute, title, domain, rating, reviews_count

Endpoint B — Business Data my_business_info (GBP review count + rating):
POST https://sandbox.dataforseo.com/v3/business_data/google/my_business_info/task_post
Body: [{"keyword": "Miami Smiles Dental", "location_name": "Miami,Florida,United States", "language_name": "English"}]
This is async (task_post + task_get). For sandbox verification, confirming task_post returns 20000 is sufficient.
Then call: GET https://sandbox.dataforseo.com/v3/business_data/google/my_business_info/tasks_ready
to check if results are ready, then retrieve.

Content-Type header required: application/json
Timeout: set to 60 seconds (live endpoints can take up to 30s)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify credentials and test Maps SERP endpoint</name>
  <files>
    knowledge/data4seo-test.json
  </files>
  <action>
    Step 1 — Confirm credentials exist in environment:
    Run: echo "LOGIN=${DATA4SEO_LOGIN:+SET} PASSWORD=${DATA4SEO_PASSWORD:+SET}"
    If either is empty/unset, stop and report: "DATA4SEO credentials not found in environment. Set DATA4SEO_LOGIN and DATA4SEO_PASSWORD before proceeding."

    Step 2 — Test Maps SERP endpoint (sandbox):
    Run this Python script from the project root:

    python3 << 'EOF'
    import os, json, base64, urllib.request

    login = os.environ["DATA4SEO_LOGIN"]
    password = os.environ["DATA4SEO_PASSWORD"]
    creds = base64.b64encode(f"{login}:{password}".encode()).decode()

    payload = json.dumps([{
        "keyword": "dentist miami",
        "location_code": 1015116,
        "language_code": "en",
        "device": "desktop",
        "os": "windows"
    }]).encode()

    req = urllib.request.Request(
        "https://sandbox.dataforseo.com/v3/serp/google/maps/live/advanced",
        data=payload,
        headers={"Authorization": f"Basic {creds}", "Content-Type": "application/json"},
        method="POST"
    )
    with urllib.request.urlopen(req, timeout=60) as resp:
        data = json.loads(resp.read())

    task = data["tasks"][0]
    print("status_code:", task["status_code"])
    print("status_message:", task.get("status_message"))
    if task.get("result"):
        items = task["result"][0].get("items", [])
        print("items_count:", len(items))
        if items:
            first = items[0]
            print("first_item_keys:", list(first.keys()))
    EOF

    Expected output: status_code: 20000

    Record the full response (or a trimmed version showing status_code, items count, and first item keys) for Task 2.
  </action>
  <verify>
    <automated>python3 -c "
import os, json, base64, urllib.request
login=os.environ['DATA4SEO_LOGIN']; pw=os.environ['DATA4SEO_PASSWORD']
creds=base64.b64encode(f'{login}:{pw}'.encode()).decode()
payload=json.dumps([{'keyword':'dentist miami','location_code':1015116,'language_code':'en'}]).encode()
req=urllib.request.Request('https://sandbox.dataforseo.com/v3/serp/google/maps/live/advanced',data=payload,headers={'Authorization':f'Basic {creds}','Content-Type':'application/json'},method='POST')
with urllib.request.urlopen(req,timeout=60) as r: d=json.loads(r.read())
sc=d['tasks'][0]['status_code']; print('PASS' if sc==20000 else f'FAIL: {sc}')
"</automated>
  </verify>
  <done>Maps SERP sandbox returns status_code 20000. Items array is present in the response.</done>
</task>

<task type="auto">
  <name>Task 2: Test Business Data endpoint and write knowledge/data4seo-test.json</name>
  <files>
    knowledge/data4seo-test.json
  </files>
  <action>
    Test the Business Data my_business_info endpoint (sandbox):

    python3 << 'EOF'
    import os, json, base64, urllib.request

    login = os.environ["DATA4SEO_LOGIN"]
    password = os.environ["DATA4SEO_PASSWORD"]
    creds = base64.b64encode(f"{login}:{password}".encode()).decode()

    payload = json.dumps([{
        "keyword": "Miami Smiles Dental",
        "location_name": "Miami,Florida,United States",
        "language_name": "English"
    }]).encode()

    req = urllib.request.Request(
        "https://sandbox.dataforseo.com/v3/business_data/google/my_business_info/task_post",
        data=payload,
        headers={"Authorization": f"Basic {creds}", "Content-Type": "application/json"},
        method="POST"
    )
    with urllib.request.urlopen(req, timeout=60) as resp:
        data = json.loads(resp.read())

    task = data["tasks"][0]
    print("status_code:", task["status_code"])
    print("task_id:", task.get("id"))
    print("status_message:", task.get("status_message"))
    EOF

    Expected: status_code 20000 (task accepted). The task_id confirms the async job was queued.

    After both tests pass, write /Users/erick/claude-examples/des-intelligence-platform/knowledge/data4seo-test.json:

    {
      "_meta": {
        "verified": "2026-05-20",
        "environment": "sandbox (sandbox.dataforseo.com)",
        "note": "Switch base URL to api.dataforseo.com for production. Sandbox returns mock data, no credits consumed.",
        "credentials_env": ["DATA4SEO_LOGIN", "DATA4SEO_PASSWORD"]
      },
      "tests": [
        {
          "endpoint": "POST /v3/serp/google/maps/live/advanced",
          "purpose": "Local keyword rankings + competitor list for /des-audit",
          "test_keyword": "dentist miami",
          "location_code": 1015116,
          "status_code": 20000,
          "items_returned": "<actual count from response>",
          "sample_fields": ["rank_absolute", "title", "domain", "rating", "reviews_count"],
          "result": "PASS"
        },
        {
          "endpoint": "POST /v3/business_data/google/my_business_info/task_post",
          "purpose": "GBP review count and rating for /des-audit",
          "test_keyword": "Miami Smiles Dental",
          "location": "Miami,Florida,United States",
          "status_code": 20000,
          "task_id": "<actual task_id from response>",
          "result": "PASS"
        }
      ],
      "usage_notes": {
        "maps_serp_cost": "$0.001-0.002 per call (production)",
        "business_data_cost": "$0.001 per task (production)",
        "auth_pattern": "base64(DATA4SEO_LOGIN + ':' + DATA4SEO_PASSWORD) in Authorization: Basic header",
        "timeout_recommended": 60,
        "domain_matching": "Normalize domains: strip www. and trailing slash before matching items[].domain"
      }
    }

    Fill in actual values (items_returned, task_id) from the live sandbox responses. If either test returns a non-20000 status_code, set result to "FAIL" and record the actual status_code and status_message for diagnosis.

    After writing the file, commit:
    git add knowledge/data4seo-test.json
    git commit -m "feat(foundation): verify Data4SEO sandbox — both endpoints confirmed"
  </action>
  <verify>
    <automated>cd /Users/erick/claude-examples/des-intelligence-platform && python3 -c "import json; d=json.load(open('knowledge/data4seo-test.json')); results=[t['result'] for t in d['tests']]; print('ALL PASS' if all(r=='PASS' for r in results) else 'FAILURES: '+str(results))"</automated>
  </verify>
  <done>knowledge/data4seo-test.json exists with two test entries, both showing result: "PASS" and status_code: 20000. File is committed to git.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env vars → HTTP Basic Auth | DATA4SEO credentials passed as base64 Basic auth header to sandbox |
| sandbox response → local JSON | Mock data from Data4SEO sandbox written to local file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | DATA4SEO_LOGIN / DATA4SEO_PASSWORD in env | mitigate | Credentials never written to any file; only base64-encoded in HTTP header at call time |
| T-04-02 | Information Disclosure | knowledge/data4seo-test.json | accept | Contains only mock sandbox response metadata and endpoint docs — no credentials |
| T-04-03 | Spoofing | Sandbox vs production URL | mitigate | Plan explicitly uses sandbox.dataforseo.com; usage_notes in JSON records the production URL switch requirement |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | Uses stdlib only (urllib, json, base64, os) — no new package installs |
</threat_model>

<verification>
1. `echo "LOGIN=${DATA4SEO_LOGIN:+SET} PASSWORD=${DATA4SEO_PASSWORD:+SET}"` — both report SET
2. `python3 -c "import json; d=json.load(open('knowledge/data4seo-test.json')); print([t['result'] for t in d['tests']])"` — prints ['PASS', 'PASS']
3. `git log --oneline -1` — shows the Data4SEO verification commit
</verification>

<success_criteria>
Data4SEO sandbox returns valid results (status_code 20000) for both Maps SERP and Business Data endpoints using the environment credentials. knowledge/data4seo-test.json records the test results. The /des-audit implementer in Phase 4 can read this file to understand the exact request shape and response fields without re-discovering the API.
</success_criteria>

<output>
Create .planning/phases/01-foundation/01-04-SUMMARY.md when done, documenting: both test results (PASS/FAIL), actual status codes received, items count from Maps SERP, task_id from Business Data, commit hash, and any deviations (wrong credentials, network error, unexpected response shape).
</output>
